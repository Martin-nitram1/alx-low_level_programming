#include "main.h"
/**
 * _islower - Searches for lowercase characters
 * @c: is the character to check
 * Return: 1 if lowercase and 0 if otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
